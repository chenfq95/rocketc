# @rocketc/react

> Deprecated: this package is no longer maintained and its implementation has been removed.

Use [`@rocketc/design-system`](https://www.npmjs.com/package/@rocketc/design-system) for Rocketc design tokens, CSS themes, and framework adapters.

New code must not depend on `@rocketc/react`. Existing consumers should migrate their visual foundations to `@rocketc/design-system` and use the appropriate component library for their framework.
