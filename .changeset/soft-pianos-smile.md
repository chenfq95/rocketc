---
'@rocketc/web-components': minor
---

为所有组件增加通过 Shadow Root CSS 规则注入的通用宿主样式属性（包括轴向间距 `px`、`py`、`mx`、`my`）和语义 CSS Parts，统一布局容器的默认表面样式，并构建、导出单文件压缩版 `index.min.js`。

Add common host-style properties injected through a Shadow Root CSS rule, including axis spacing through `px`, `py`, `mx`, and `my`, semantic CSS parts for every component, consistent default surfaces for layout containers, and a minified single-file `index.min.js` bundle.
