---
'@rocketc/web-components': patch
---

补齐 `rc-button` 的原生表单关联、提交、重置、约束校验、焦点与激活行为，支持表单提交覆盖属性、链接按钮、命令和弹出框目标；`rc-icon-button` 现在共享相同 API，并使所有表单关联组件正确响应禁用的 `fieldset`。
Add native form association, submission, reset, constraint validation, focus, and activation behavior to `rc-button`; support form submission overrides, link buttons, commands, and popover targets; make `rc-icon-button` share the same APIs; and make all form-associated components respond correctly to disabled fieldsets.
