---
'@rocketc/web-components': patch
---

补齐 `rc-button` 的原生表单关联、提交、重置、约束校验、命令和弹出框属性，公开表单所有者与关联标签，并使所有表单关联组件正确响应禁用的 `fieldset`。
Add native form association, submission, reset, constraint validation, command, and popover attributes to `rc-button`, expose its form owner and associated labels, and make all form-associated components respond correctly to disabled fieldsets.
